
import React from 'react';

const Header = () => {
  return (
    <div className="text-center mb-8">
      {/* Header is now empty - just maintaining spacing */}
    </div>
  );
};

export default Header;
